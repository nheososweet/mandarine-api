DATABASE_URL=postgresql://postgres:123456@localhost:5432/mandarine_ecosystem_db
# --- 1. CẤU HÌNH CHUNG ---
PROJECT_NAME="Mandarine API"
API_V1_STR="/api/v1"
# Quan trọng: Dùng để tạo link invite (VD: http://localhost:8000/api/v1/workspaces/join?token=...)
SERVER_HOST="http://localhost:8000"

# --- 2. BẢO MẬT (SECURITY) ---
# Chạy lệnh 'openssl rand -hex 32' để sinh chuỗi ngẫu nhiên, hoặc gõ bừa một chuỗi dài
SECRET_KEY="thay_bang_chuoi_bi_mat_sieu_dai_va_ngau_nhien_cua_ban"
ALGORITHM="HS256"
# Thời gian hết hạn Token (phút). Để 1 tuần (10080) cho đỡ phải login lại nhiều khi dev
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# --- 3. DATABASE (POSTGRESQL) ---
# Định dạng: postgresql://<user>:<password>@<host>:<port>:<db_name>
# Ví dụ dưới đây là user 'postgres', pass '123456', db 'mandarine'

# --- 4. EMAIL (SMTP GMAIL) ---
# Nếu dùng Gmail, bạn PHẢI dùng "App Password", không phải mật khẩu đăng nhập
MAIL_USERNAME="email_cua_ban@gmail.com"
MAIL_PASSWORD="mat_khau_ung_dung_16_ky_tu" 
MAIL_FROM="email_cua_ban@gmail.com"
MAIL_PORT=587
MAIL_SERVER="smtp.gmail.com"
MAIL_STARTTLS=True
MAIL_SSL_TLS=False
USE_CREDENTIALS=True
VALIDATE_CERTS=True